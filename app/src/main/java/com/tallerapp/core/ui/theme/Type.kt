package com.tallerapp.core.ui.theme

import androidx.compose.material3.Typography

// Tipografía base (Material 3 por defecto). Se ajustará en Fase 8 (Pulido de UX).
val TallerTypography = Typography()
